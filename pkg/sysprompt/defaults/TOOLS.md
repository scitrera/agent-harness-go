# TOOLS.md

Workspace-specific notes on how to use the available tools — conventions,
preferred tools for common tasks, and any caveats. The assistant reads this
alongside the live tool list.

Edit to add guidance, e.g. "prefer `edit_file` over `write_file` for small
changes" or "always `inspect_file` before reading large binaries".
